T =float(input("Введите температуру в °C: "))

F = T * 9/5 + 32 #Формула

if F <= 0:
    state = "Лед"
elif F >= 100:
    state = "Пар"
else:
    state = "Жидкость"

print(f"== КОНВЕРТЕР ТЕМПЕРАТУР ==\n"
      f"Температура в °C: {T}\n"
      f"Температура в F: {F}\n"
      f"Состояние воды: {state}")
